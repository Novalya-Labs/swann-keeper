/**
 * Swann — TTS module barrel.
 *
 * Exposes the offline text-to-speech service used to speak agent replies in the
 * voice channel. See ttsService.ts.
 */

export { createTtsService, type TtsService } from './ttsService.js';
